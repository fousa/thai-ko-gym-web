- if can?(:manage, @user)
  - content_for :title, t('pages.users.form.titles.edit')
- else
  - content_for :title, t('pages.users.form.titles.show')

- content_for :toolbar do
  = back_tag users_path

= render 'form'
